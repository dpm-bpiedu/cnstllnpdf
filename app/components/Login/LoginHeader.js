import React from 'react';

const LoginHeader = (props) => {
  return (
    <React.Fragment>
      
      <p className="lead">This application creates pdf files from html. Upload html files to CDN. Log in to send request to docraptor.com and receive pdf files.</p>
    </React.Fragment>
  )
}

export default LoginHeader;

